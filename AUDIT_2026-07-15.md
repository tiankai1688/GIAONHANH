# GIAONHANH 开发计划漏洞审计 · 2026-07-15

> 审计范围：消费者端 / 商家端 / 骑手端 / Laravel API / 支付 IPN / 原生壳
> 结论：后端鉴权、消费者入口、运行时验证三处为 **P0 阻断级**；派单模型冲突、退款、实时推送为 **P1/P2**；财务/合规/测试为 **P3**。

## 已扎实的部分（不要推翻）
- 支付 IPN 验签（MoMo / ZaloPay / 聚合）已端到端验证（`tools/verify-ipn.mjs` + `backend/scripts/verify-ipn.php`）
- 0 佣金 + 配送补贴分账模型已在 `PaymentSplitService` 落地，`delivery_fee` 在下单时正确计算
- 商家端 / 骑手端已接后端实时数据，`LIVE/src*/map*/tryLive` 离线降级模式健壮
- `Order` 路由键改为 `order_no`，贯通三端 `{order}` 端点
- 支付合规文档 `backend/docs/PAYMENT_COMPLIANCE.md`（禁止二清、须持牌聚合）

---

## P0 — 阻断级（不修则 live 联调整体不可行）

### P0-1 · `ability` 中间件未注册  ⛔ 已确认
- 现象：全仓 `app/ bootstrap/ config/` 搜 `ability` 返回空；`app/Http/Middleware` 目录为空；`bootstrap/app.php` 仅 append `HandleCors`。所有 `->middleware(['auth:sanctum','ability:customer|merchant|rider|admin'])` 路由运行即抛 `middleware [ability:xxx] not found` → 三端全部 500。
- 修复：
  1. 新建 `app/Http/Middleware/EnsureTokenHasAbility.php`：`handle($req,$next,...$abilities){ if(!$req->user()||!$req->user()->tokenCan($a)) abort(403); return $next($req);}`
  2. `bootstrap/app.php` 注册别名：`$middleware->alias(['ability' => EnsureTokenHasAbility::class]);`
  - 注意：Sanctum token 发放时要 `Token::create(['abilities'=>[...]])` 写入对应能力（customer/merchant/rider/admin），否则 `tokenCan` 永远 false。

### P0-2 · 消费者端未接后端  ⛔ 已确认
- 现象：`app/index.html` `GN_CONFIG.apiBase:""` `useApi:false`，纯离线 demo。整条链路没有入口，订单永远下不到后端 → 三端 E2E 联调无法验证。
- 修复：
  1. `app/api.js` 补：`createOrder(payload)` / `payOrder(no,method)` / `myOrders()` / `orderShow(no)` / `cancelOrder(no)` / `paymentStatus(no)`
  2. `index.html` 填 `apiBase` + `useApi:true`，购物车结算 → 下单 → `pay` → 轮询 `payment-status` 全链路打通
  3. 地址收集时取 lat/lng（`native.getLocation()` 已有），否则 `dispatchRider` 拿不到坐标

### P0-3 · 后端从未运行  ⛔ 环境限制
- 现象：沙箱无 PHP/Composer，`migrate --seed` 未跑过。路由绑定(order_no)、模型作用域(`approved/available/flash`)、`effectivePrice()`、`PaymentSplitService::distance`、ability 全部未经验证。
- 修复：拿到 PHP 8.2+ / Composer 环境，`php artisan migrate --seed` 后做一次 runtime smoke test：注册三端账号 → 下单 → COD → 看自动派单 → 商家接单 → 骑手抢单 → 送达 → 看收益。

---

## P1 — 核心流程正确性

### P1-1 · 自动派单 vs 抢单模型冲突  ⚠️ 已确认代码
- 现象：`dispatchRider()` 在支付成功（IPN / COD）即把最近在线骑手写入 `rider_id`，但骑手屏是"抢单"UX，且 `rider/orders` 还返回 `whereNull('rider_id')->where('status','picked')`。两套逻辑打架，自动派单后别的骑手看不到该单，被分配骑手又处于"待抢"状态，状态机混乱。
- 修复（二选一，推荐 A）：
  - **A 抢单制（更像美团）**：移除支付时自动派单；改为 商家 `ready`(picked) 后，把订单广播给附近在线骑手，由 `rider/accept` 抢占写入 `rider_id`。
  - **B 自动派单制**：骑手屏去掉"抢单"，改为"我的任务"列表直接展示系统指派。

### P1-2 · 取消订单不退款（钱包支付）  ⚠️
- 现象：`OrderController::cancel()` 只置 `cancelled`。MoMo/ZaloPay 已支付订单不退 → 资金/合规风险。
- 修复：cancel 时若 `payment.status=success && method!=cod`，调用网关 refund API，订单加 `refunding/refunded` 状态 + 退款记录。

### P1-3 · CORS / mock 页暴露  ⚠️
- 现象：`HandleCors` 已挂但未确认 `config/cors.php` 白名单；`backend/public/pay-mock.html` 公开可访问。
- 修复：明确 `allowed_origins`（Capacitor 用 `capacitor://localhost` 或你的域名）；生产移除 mock 页或加守卫。

---

## P2 — 实时 & 原生体验（决定"像不像美团"）

### P2-1 · 推送通知半残  ⚠️ 已确认
- 现象：`native-bridge.js` 的 `registerPush` 抓到 token 但**未上传后端、后端无存储表、无发送逻辑** → 死代码。
- 修复：`device_tokens` 表 + 上传端点；`registerPush` 把 token POST 后端；新订单/可抢单经 FCM/APNs 下发。

### P2-2 · 无实时通道  ⚠️
- 现象：商家/骑手靠手动刷新轮询，新订单无即时触达。
- 修复：上 WebSocket（Pusher / Ably / Soketi + Laravel Echo），新订单事件广播商家，可抢单广播附近骑手。

### P2-3 · 无地图 / 地理编码 / 导航  ⚠️ 已确认
- 现象：地址自由文本，仅 Haversine 算距离（需 lat/lng 就位）；骑手屏只有距离数字，无取送点/路线/ETA。
- 修复：集成地图（越南 Map4D / Google Maps），下单地址→lat/lng 地理编码，骑手屏显示路线+ETA+导航拉起。

---

## P3 — 业务 / 运营 / 合规 / 质量

- **P3-1 管理后台过薄**：仅商户审批 + 代理列表。补 结算/对账视图（0 佣金也要跟踪商户应收 + 平台补贴支出）。
- **P3-2 KYC/AML/PDPD 仅文档**：商户营业执照+对公账户核验、骑手身份核验（接 VN eKYC）；隐私政策 + 同意 + 数据删除。
- **P3-3 零自动化测试**：Pest 覆盖 IPN 验签 / 分账 / 派单 / 状态机。
- **P3-4 iOS 未构建**：需 Mac 跑 `npx cap add ios`，配 APNs 证书。

---

## 建议的补齐顺序
1. **P0-1 + P0-2**（我可在本环境直接写代码，不依赖 PHP 运行）
2. **P0-3**（需你提供 PHP/Composer 环境跑通验证）
3. **P1-1 派单模型决策**（需你拍板 A 抢单 / B 自动派单）
4. **P1-2 退款 + P2-1/P2-2 推送/实时**
5. **P2-3 地图 → P3 财务/合规/测试/iOS**
