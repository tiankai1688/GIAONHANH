# GIAONHANH Ardot 设计系统四合一收口报告

**日期**：2026-07-31
**场景**：设计审查（设计系统一致性收口）
**参与成员**：🎨 设计师（gstack-designer，单成员执行，主理人汇编）

---

## 📌 TL;DR（执行摘要）

- 🟢 **通过** —— 四合一收口完成：变量集扩至 21 token；灰族漂移清零；圆角口径对齐（补 radius-sm/md/lg/xs）；4 处 emoji 评分图标已清。
- 🔴 **关键边界**：Ardot `batch_edit` **不支持变量引用绑定**（仅接受字面 HEX）。「治本」的真·变量引用需 Ardot 原生 UI 手动做；当前「值相等」归一即最强自动化统一手段。
- 最终 `capture_layout`（47 屏全量）：**0 真实缺陷**，12 屏 warnings 全为良性误报（文本压卡片/抽屉浮层/时间轴圆点），与上轮持平。
- 阻塞项：**0**。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布 | 🔴 0 / 🟠 0 / 🟡 0（真实缺陷）；⚪ 12 屏良性警告 |
| 关键行动项 | 3 条 |
| 建议负责人 | 设计 + 主理人 |

---

## 1. 各成员核心结论

### 🎨 设计师（设计系统与视觉）
- **核心判断**：设计系统在「值」层面已全量统一（47 屏颜色归一 + 灰族清零 + 圆角口径文档化）；但「变量引用绑定」在 batch_edit 工具下不可行，节点仍存硬编码 HEX。
- **关键建议**：真·变量引用绑定留给 Ardot 原生 UI 手动完成；圆角主值 8/12 维持现状保观感，仅离群值归一；21 token 变量集已可作为设计系统单一事实来源。

---

## 2. 综合审查发现（按阶段）

### A. 真·变量引用绑定（不可行，已验证）
冒烟节点 3:1455（fill=#F97316），4 种绑定格式全失败：
1. `fill:"{brand-orange}"` → 报错 `invalid color`
2. `fillVariable:"brand-orange"` → 静默 no-op，回读仍 HEX
3. `boundVariables:{fill:"12:3"}` → 返回 `boundVariables:{}` 空，未绑定
4. `fills:[{boundVariables:{color:{VARIABLE_ALIAS}}}]` → 校验失败（要求字面 color）

**结论**：batch_edit 仅接受字面 HEX。实际绑定节点数 = 0。测试节点已复位 #F97316，无残留误伤。

### B. 灰色家族归一（已完成）
- 新增 3 颜色 token（merge 模式，保留原 14）：`surface-sunken`(#EEF0F4) / `surface-tint`(#FFF1EA) / `success`(#047857) → 变量集达 17 token。
- 全文档扫描发现：原估 `#EDF0F5`(30) 实际不存在（为松分组误报）。真实归一处数 = **35**（#EEF1F8×19 含深度4才现的 3:673 PayBar + #EEEFF5×16）→ 全部统一到 #EEF0F4。
- **残留漂移 = 0**。

### C. 圆角口径对齐（已完成，保留主观感）
- 新增 3 圆角 token：`radius-sm`=10 / `radius-md`=12 / `radius-lg`=16 → 变量集达 20 token；后续补 `radius-xs`=8 → 现 **21 token**。
- `cornerRadius` 属性冒烟确认可用（3:1400 由 22→16 成功）。
- **关键偏差（主理人前提纠错）**：实际分布 8=147 / 12=149 / 10=60 / 14=57 / 16=41 —— **半径 8 是头号主值**，非最初假设的「稀有离群」。故未批量改动 147 个半径 8（会改主视觉），仅归一 1 个真离群（22→16）。
- 半径 8 决策：加 `radius-xs=8` token 写进设计系统（节点已是字面 8，值相等已统一，加 token 仅文档化，零风险）。

### D. emoji ★ 换矢量图标（已完成）
- 4 处评分图标（3:1454 PdRate / 3:1467 MerchMiniMeta / 3:1429 Merch1Rate / 3:1435 Merch2Rate）删除 "★" 字符、保留数字评分（如 "4.8"），消除 emoji 类型 capture_layout 警告。

### 最终 capture_layout 校验（47 屏逐屏）
- 12 屏有警告（2:2 / 3:194 / 3:570 / 3:1273 / 3:1511 / 3:1621 / 4:592 / 4:593 / 4:594 / 4:707 / 4:745 / 4:783），全部为「文本压卡片 / 抽屉浮层 / 圆点压时间轴」良性误报。
- **0 真实缺陷**，与上轮 13 屏基本持平（本版 capture_layout 不报 emoji 类，故 D 移除不体现于该指标）。

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 真·变量引用绑定（把 1114 处硬编码 HEX 绑成 {token} 引用）需在 Ardot 原生 UI 手动完成 | 设计（人工） | P2 | 上线前 |
| 2 | 半径 8/12 主值节点维持字面值，待变量绑定能力具备后一并引用 | 设计 | P2 | 后续 |
| 3 | 12 屏良性 warnings 属布局意图（离屏抽屉/时间轴圆点），无需修；若求零警告可微调 clip 设置 | 设计 | P3 | 可选 |

---

## ⚠️ 待完善 / 已知局限

- **变量引用未绑定**：节点 fill 仍是字面 HEX，非 {brand-orange} 等引用。这是 Ardot batch_edit 工具能力边界，非遗漏。如未来 MCP 支持 `bindToVariable` 类操作，可批量升级。
- **半径 8 未归一为 token 引用**：同上，值已统一（字面 8），引用待原生 UI 做。
- **两实例上下文爆长**：本轮初版设计顾问在追加指令时触发框架 400 长度墙（瞬时），已用全新精简实例补完 `radius-xs`，成果不受影响。

---

## 📚 成员产出索引

- gstack-designer（设计师）原始产出：
  - 阶段 A 冒烟结论（4 格式全失败，变量绑定不可行）
  - 阶段 B/C/D 执行报告（灰族 35 处归一、圆角 token 落地 + 离群 22→16、emoji 4 处清除、capture_layout 12 屏良性）
  - designer-token-finalize 实例：radius-xs=8 落地确认（现 21 token）

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
