# GIAONHANH 三阶段交付路线图（融资前 → 真闭环 → 规模化）

> 全流程与动态进度追踪。更新日期：2026-08-02（v2：三色标注）
> 配套：原生代码交付包说明见仓库根 `NATIVE_CODE.md`

## 颜色图例（三色）
- 🟢 **绿色 = 已完成**：原生代码已落地，无需再写，可直接交付客户。
- 🟡 **黄色 = 待我个人人为完成**：需你侧动作 / 决策 / 签约 / 凭证（代码无法替代）。
- 🔴 **红色 = 未完成（工程待做）**：后续由工程实现，尚未构建。

---

## 阶段总览

| 阶段 | 名称 | 交付标准（门槛） | 当前进度 |
|---|---|---|---|
| **第一阶段** | 融资前技术就绪（杠A） | 后端真跑过（CI 绿证 + 真机视频）、压测有数据、BP 诚实、营收模型定 | 🟢 5 完成 · 🟡 3 待你 · 🔴 0 工程 |
| **第二阶段** | 越南真闭环公测（杠B） | 原生壳/强化 PWA、真实 OTP、PSP 签约、结算真出款、半自动调度、网关/可观测/回滚 | 🟢 5 完成 · 🟡 4 待你 · 🔴 5 工程 |
| **第三阶段** | 300M 规模化就绪（杠C） | 分库分片、Redis 集群、调度算法、数据本地化、服务拆分 | 🟢 0 完成 · 🟡 3 待你 · 🔴 5 工程 |

> 距离评估：第一阶段 ≈ 1 周（仅卡你侧 3 个动作）；第二+第一阶段 ≈ 6–12 周；到第三阶 ≈ 再加 8–16 周（总计 3.5–7 个月）。

---

## 第一阶段 · 融资前技术就绪（杠A）

### 交付标准
- 后端**真实跑过**并留证据：CI 四 job（contract / laravel / pest / backend-smoke）全绿 + 真机演示视频。
- 有**压测数据**（单体吞吐 / 瓶颈）作为 scale readiness 的诚实证据。
- BP / 融资叙事**诚实**（不称已上架 / 已分布式），营收模型已定。

### 能实现的效果
- 🟢 **软件完成状态**：业务正确性已成熟——支付 fail-closed 验签、订单/库存/结算逻辑、安全基线、12 个测试、CI 配置、压测脚本、BP 草案、就绪 Checklist 全部已入仓库。
- 🟡 **待我个人人为完成**：① push 触发 CI 拿绿证；② Docker 起服务录真机视频；③ 营收模型拍板。

### 待工程完成（🔴）
- 无（本阶段工程已全部完成，仅余你侧动作）。

### 完成清单（🟢）
- 🟢 底层工程（支付 / 安全 / 订单 / 结算 / 12 测试）— 已落地
- 🟢 压测脚本（`tools/load-test.k6.js` + `tools/load-test.mjs`）— 已写并验证
- 🟢 BP 诚实叙事（`docs/bp-honest-narrative.md`）— 已写
- 🟢 融资就绪 Checklist（`docs/fundraising-readiness-checklist.md`）— 已写
- 🟢 CI 配置（`.github/workflows/ci.yml` 四 job）— 已写

### 待我个人人为完成（🟡）
- 🟡 push 触发 CI 拿绿证 — 需你 `git push`
- 🟡 真机演示视频 — 需你本地 Docker 录屏
- 🟡 营收模型决策 — 需你拍板佣金% 或增值服务

---

## 第二阶段 · 越南真闭环公测（杠B）

### 交付标准
- 客户端可上架/可安装（原生壳 APK / TestFlight，或强化 PWA）。
- 注册 OTP **真实短信**送达；支付接 **PSP 生产密钥**；结算走 **真实出款 API**（不再台账模式）。
- **半自动调度**（geohash / RTree + 实时 GPS + ETA + 自动派单）。
- 基础 **API 网关 + WAF + CDN**；**可观测性**（metrics / tracing / 日志 / 告警）；**回滚**机制。

### 能实现的效果
- 🟢 **软件完成状态**：模块化单体骨架、支付/安全/订单核心链路、CSP、nearby 地理查询、调度接口位已预留。
- 🟡 **待我个人人为完成**：PSP 商务签约 + 生产密钥、真实短信网关签约、原生上架账号（Google Play / TestFlight）、出款 API 凭证。

### 待工程完成（🔴）
- 🔴 原生壳 Capacitor 工程 + 出包（代码；上架账号属黄色）
- 🔴 半自动调度引擎（geohash / RTree + GPS + ETA + 自动派单）
- 🔴 API 网关 + WAF + CDN
- 🔴 可观测性（Prometheus / OTel / 日志 / 告警）
- 🔴 回滚机制

### 完成清单（🟢）
- 🟢 安全基线（HttpOnly 刷新令牌 / CSP 哈希 / PII 加密 / OTP 门控）
- 🟢 支付核心（接口 + 验签 + 对账 + PSP 费用入账）
- 🟢 订单 / 库存 / 结算逻辑（含跨店合并、0 佣金配置化）
- 🟢 调度查询基础（`nearby` 包围盒 + 地理接口预留）
- 🟢 CI / 压测 / 文档（第一阶段产物）

### 待我个人人为完成（🟡）
- 🟡 真实短信网关签约（Twilio / 越南本地 SMS）
- 🟡 PSP 商务签约 + 生产密钥（MoMo / ZaloPay）
- 🟡 原生上架账号（Google Play / TestFlight）
- 🟡 结算出款 API 凭证

---

## 第三阶段 · 300M 规模化就绪（杠C）

### 交付标准
- 数据层：**分库分表 / 只读副本 / Redis 集群**。
- 调度层：**ML 调度**（ETA 预测、动态定价）。
- 合规层：越南数据**本地化 + 多区容灾（DR）**。
- 架构层：热路径**服务拆分**（调度/结算/通知独立部署 + 独立 DB）。
- 工程层：混沌工程、SLO、on-call。

### 能实现的效果
- 🟢 **软件完成状态**：模块化单体已为拆分埋好缝（`PaymentGatewayInterface`、`CreateOrderAction`、`Services/`）。
- 🟡 **待我个人人为完成**：规模化运营预算、越南本地数据中心 + 合规资源、ML / 数据团队招募。

### 待工程完成（🔴）
- 🔴 数据分片 + 只读副本 + Redis 集群
- 🔴 ML 调度（ETA / 动态定价）
- 🔴 数据本地化 + 多区 DR（工程部分）
- 🔴 热路径服务拆分（调度 / 结算 / 通知）
- 🔴 混沌工程 / SLO / on-call

### 完成清单（🟢）
- （本阶段尚未启动，目前无绿色项）

### 待我个人人为完成（🟡）
- 🟡 规模化运营预算
- 🟡 越南本地数据中心 + 合规资源
- 🟡 ML / 数据团队招募

---

## 动态进度维护
- 每完成一项，按性质改色：工程做完 → 🔴 改 🟢；你侧动作做完 → 🟡 改 🟢；并注明日期。
- 🟢 已完成（原生代码可交付）｜🟡 待你个人人为完成｜🔴 工程待做。
- 完整原生代码交付包见根目录 `NATIVE_CODE.md`。
