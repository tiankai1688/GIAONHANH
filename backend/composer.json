{
    "name": "giaonhanh/backend",
    "type": "project",
    "description": "GIAONHANH - Vietnam on-demand delivery platform backend (Laravel API)",
    "keywords": ["laravel", "vietnam", "delivery", "ecommerce", "api"],
    "license": "MIT",
    "require": {
        "php": "^8.2",
        "laravel/framework": "^11.0",
        "laravel/sanctum": "^4.0",
        "laravel/tinker": "^2.9",
        "doctrine/dbal": "^3.8"
    },
    "require-dev": {
        "fakerphp/faker": "^1.23",
        "phpunit/phpunit": "^11.0",
        "laravel/pint": "^1.13",
        "pestphp/pest": "^3.0",
        "pestphp/pest-plugin-laravel": "^3.0",
        "mockery/mockery": "^1.6"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Database\\Seeders\\": "database/seeders/",
            "Database\\": "database/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "post-autoload-dump": [
            "@php -r \"is_dir('bootstrap/cache')||@mkdir('bootstrap/cache',0777,true);is_dir('storage/app/public')||@mkdir('storage/app/public',0777,true);is_dir('storage/framework/cache/data')||@mkdir('storage/framework/cache/data',0777,true);is_dir('storage/framework/sessions')||@mkdir('storage/framework/sessions',0777,true);is_dir('storage/framework/views')||@mkdir('storage/framework/views',0777,true);is_dir('storage/framework/testing')||@mkdir('storage/framework/testing',0777,true);is_dir('storage/logs')||@mkdir('storage/logs',0777,true);\"",
            "Illuminate\\Foundation\\ComposerScripts::postAutoloadDump",
            "@php artisan package:discover --ansi"
        ],
        "post-update-cmd": [
            "@php artisan vendor:publish --tag=laravel-assets --ansi --force"
        ],
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ],
        "post-create-project-cmd": [
            "@php artisan key:generate --ansi"
        ]
    },
    "extra": {
        "laravel": {
            "dont-discover": []
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true,
        "policy": {
            "advisories": {
                "block": false
            }
        },
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "php-http/discovery": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
